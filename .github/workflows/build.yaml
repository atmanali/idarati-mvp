name: build app

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
  
  steps:
    - uses: actions/checkout@v4
    - name: uses node js
      uses: actions/setup-node@v4
      with:
        node-version: 22.11.0
    - run: |
        npm ci
        npm build
